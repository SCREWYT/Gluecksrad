<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gluecksrad">

    <!-- Erlaubt Internetzugriff -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gluecksrad"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- MainActivity – NICHT der Launcher, wird nach Login geöffnet -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false" />

        <!-- RegisterActivity – erreichbar vom Login aus -->
        <activity
            android:name=".ui.RegisterActivity"
            android:exported="false" />

        <!-- LoginActivity – STARTSCREEN der App -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Weitere Screens -->
        <activity android:name=".ui.UpgradeActivity" android:exported="false" />
        <activity android:name=".ui.SettingsActivity" android:exported="false" />

    </application>

</manifest>
