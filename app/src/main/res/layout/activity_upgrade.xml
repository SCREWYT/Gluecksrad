<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center_horizontal">

    <!-- Titel -->
    <TextView
        android:id="@+id/textViewUpgradeTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Upgrades"
        android:textSize="28sp"
        android:textStyle="bold"
        android:layout_marginBottom="24dp" />

    <!-- SP = Scale Independent Pixels -->

    <!-- Aktueller Score -->
    <TextView
        android:id="@+id/textViewUpgradeStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Dein Punktestand: 0"
        android:textSize="18sp"
        android:layout_marginBottom="24dp" />

    <!-- 0 = Platzhalter erneut wie davor bei "activity_main" und dem Rad -->

    <!-- Upgrade: Multiplikator -->
    <Button
        android:id="@+id/buttonUpgradeMultiplier"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Multiplikator x1.0 → x1.1\nKosten: 100"
        android:layout_marginBottom="32dp" />

    <!-- Upgrade: Feldwerte -->
    <Button
        android:id="@+id/buttonUpgradeFieldValue"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Feldwerte: ±5 stärker\nKosten: 3"
        android:layout_marginBottom="16dp" />

    <!-- Zurück-Button zum Mainscreen des Glücksrads -->
    <Button
        android:id="@+id/buttonBackToMain"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Zurück zum Rad" />

</LinearLayout>
